% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finalize_sib_prep.R
\name{finalize_sib_prep}
\alias{finalize_sib_prep}
\title{shared tail of the sibling-history prep functions}
\usage{
finalize_sib_prep(
  ego.dat,
  sib.dat,
  cur.survey,
  miss_col,
  resp.attrib,
  keep_missing = FALSE,
  keep_varmap_only = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{ego.dat}{the prepped ego dataset}

\item{sib.dat}{the prepped sibling dataset}

\item{cur.survey}{the survey id}

\item{miss_col}{the list returned by \code{check_varmap_cols()}}

\item{resp.attrib}{named vector of ego variables from the varmap, used by
\code{keep_varmap_only}}

\item{keep_missing}{see \link{prep_dhs_sib_histories}}

\item{keep_varmap_only}{see \link{prep_dhs_sib_histories}}

\item{verbose}{see \link{prep_dhs_sib_histories}}
}
\value{
a list with entries \code{survey}, \code{ego.dat}, \code{sib.dat} and \code{summ}
}
\description{
Summarises a prepped ego/sibling pair, optionally drops sibling reports that
are missing survival status or sex, and assembles the returned list. Shared
by \link{prep_dhs_sib_histories},
\link{prep_nrsim_sib_histories} and
\link{prep_mics_sib_histories} so that the three preps cannot
drift apart.
}
\section{Details}{

Unless \code{keep_missing = TRUE}, three kinds of sibling report are dropped:
those with unknown survival status, those with unknown sex, and those with
no usable date of birth. The last cannot be placed in an age group, so they
contribute neither exposure nor events -- and left in, a single one turns an
entire exposure cell into \code{NA}, since the estimator sums over the cell.
}

