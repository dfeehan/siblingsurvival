% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_varmap_cols.R
\name{check_varmap_cols}
\alias{check_varmap_cols}
\title{report varmap columns that are missing from a dataset}
\usage{
check_varmap_cols(df, resp.attrib, sib.attrib, sep = "\\\\.|_", verbose = TRUE)
}
\arguments{
\item{df}{the survey dataset}

\item{resp.attrib}{named vector of ego (respondent) variables from the varmap}

\item{sib.attrib}{named vector of sibling variables from the varmap}

\item{sep}{regular expression separating the sibling variable prefix from the
sibling number; must match the \code{sep} passed to
\link{attributes.to.long}}

\item{verbose}{print a message describing the missing columns?}
}
\value{
a list with entries \code{ego} and \code{sib}, each a (possibly empty) named
vector of varmap columns that were not found in \code{df}
}
\description{
In some cases, variables in the varmap will not be in the specific dataset
being prepared (for example, some DHS surveys don't have the 'literacy'
variable, v155). In those cases we report the missing columns and proceed.
}
\details{
Ego (respondent) variables are matched by exact name. Sibling variables are
matched as \emph{prefixes}, since the wide-form data has one column per reported
sibling: the varmap entry \code{mm1} corresponds to columns \code{mm1_1}, \code{mm1_2},
and so on. The regular expression used here has to match the one used by
\link{attributes.to.long}, which is what actually reshapes those
columns.
}
