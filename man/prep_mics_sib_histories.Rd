% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_mics_sib_histories.R
\name{prep_mics_sib_histories}
\alias{prep_mics_sib_histories}
\title{prepare a MICS dataset for analysis}
\usage{
prep_mics_sib_histories(
  mm.df,
  survey,
  wm.df = NULL,
  varmap = sibhist_varmap_mics6,
  id.vars = c("hh1", "hh2", "ln"),
  doi.var = "wdoi",
  doi.ym = c("wm6y", "wm6m"),
  dob.var = "wdob",
  lowercase = TRUE,
  weight.scale = 1,
  add_maternal = FALSE,
  keep_missing = FALSE,
  keep_varmap_only = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{mm.df}{the MICS maternal mortality file (\code{mm.sav}), one row per
reported sibling}

\item{survey}{string identifying the survey, eg \code{"ZW2019"}. Required: MICS
has no \code{v000} equivalent, so this cannot be derived; see Details}

\item{wm.df}{optional women's file (\code{wm.sav}), joined on \code{id.vars} to bring
across respondent attributes that \code{mm.df} does not carry}

\item{varmap}{see Details; defaults to \code{sibhist_varmap_mics6}}

\item{id.vars}{columns of \code{mm.df} that together identify a respondent.
Combined to form \code{caseid}}

\item{doi.var}{column holding the date of interview as a CMC. Used when
present; otherwise \code{doi.ym} is used}

\item{doi.ym}{year and month columns from which to construct a CMC date of
interview, when \code{doi.var} is absent}

\item{dob.var}{column holding the respondent's date of birth as a CMC, used
to derive respondent age}

\item{lowercase}{lowercase all variable names before matching the varmap?
See Details}

\item{weight.scale}{divide the weight by this number. Defaults to \code{1}, since
MICS weights are already normalized; see Details}

\item{add_maternal}{should maternal/pregnancy-related death info be added?}

\item{keep_missing}{should we keep reported sibs that are missing sex or survival status?}

\item{keep_varmap_only}{should we only keep ego variables mentioned in the varmap?}

\item{verbose}{report detailed summaries?}
}
\value{
a list; see Details
}
\description{
prepare a MICS dataset for analysis
}
\section{Details}{


MICS publishes the sibling history as a dedicated file with \strong{one row per
reported sibling}, unlike the DHS, which publishes a wide women's file with
one column per sibling attribute per sibling. So no reshape is needed, and
this function does not call \link{attributes.to.long}.

\code{survey} has no default because MICS has no \code{v000} equivalent. Supplying it
explicitly keeps MICS survey ids comparable with the DHS codes.

\code{weight.scale} defaults to \code{1} because MICS weights (\code{wmweight}) are already
normalized to average 1. Do not pass \code{1e6} here: that is a DHS convention.

MICS \code{.sav} files use \strong{mixed} case -- questionnaire items and link keys are
uppercase (\code{MM15}, \code{HH1}, \code{WDOI}) while derived and design variables are
lowercase (\code{wmweight}, \code{psu}, \code{welevel}) -- so \code{lowercase = TRUE} normalizes
them and every shipped MICS varmap is written in lowercase.

Several columns the rest of the pipeline requires are not in every MICS file
and are constructed here when absent:
\itemize{
\item \code{caseid} -- from \code{id.vars}; MICS has no single respondent id
\item \code{doi} -- from \code{doi.var}, else \code{doi.ym}. Must end up a CMC
\item \code{age} -- respondent age, from \code{(doi - dob.var) / 12}
\item \code{psu} -- from the first of \code{id.vars} (the cluster) when no \code{psu} column
exists, which is the case in most MICS surveys
\item \code{sex} -- constant \code{'f'}; MICS interviews only women
}

\code{sib.dob} and \code{sib.death.date} are used when the varmap supplies them
(\code{MM17C}/\code{MM18C} in MICS6, \code{MM7C}/\code{MM8C} in MICS4/5) and approximated from
reported ages and years-since-death otherwise.

\code{varmap} has the same format as the DHS varmaps: columns \code{orig.varname},
\code{new.varname} and \code{sibvar}.

Returns a list whose entries include
\itemize{
\item \code{ego.dat} - dataset with information about the survey respondents
\item \code{sib.dat} - dataset with information about the reported siblings
\item \code{summ} - a one-row tibble with a summary of the data
}
}

\examples{
  # TODO - write example code
}
