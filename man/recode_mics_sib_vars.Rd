% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_mics_sib_histories.R
\name{recode_mics_sib_vars}
\alias{recode_mics_sib_vars}
\title{recode MICS sibling variables to the conventions the package expects}
\usage{
recode_mics_sib_vars(df, verbose = TRUE)
}
\arguments{
\item{df}{the renamed MICS sibling data}

\item{verbose}{report the recode?}
}
\value{
\code{df} with \code{sib.alive} recoded, and \code{sib.sex} validated
}
\description{
MICS and the DHS code survival status differently, and the difference is
silent: MICS \code{MM16} is 1 yes / 2 no / 8 don't know, while the DHS \code{mm2} is
1 alive / 0 dead. Everything downstream filters on \code{sib.alive \%in\% c(0,1)},
so passing MICS codes through unchanged drops \strong{every dead sibling} as
though its survival status were missing -- which silently drives every
mortality estimate to zero.
}
