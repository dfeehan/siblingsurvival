% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_sib_df.R
\name{get_sib_df}
\alias{get_sib_df}
\title{helper to prep the sib dataset}
\usage{
get_sib_df(
  ego.dat,
  sib.attrib,
  verbose = FALSE,
  reshape = TRUE,
  max.plausible.age = 110,
  death.exposure = c("dhs", "mics")
)
}
\arguments{
\item{ego.dat}{the prepped ego dataset}

\item{sib.attrib}{vector with sibling attribute columns (see \link{prep_dhs_sib_histories})}

\item{verbose}{see \link{prep_dhs_sib_histories}}

\item{reshape}{is \code{ego.dat} wide, with one column per sibling attribute per
sibling (TRUE, the DHS layout), or already one row per reported
sibling (FALSE, the MICS \code{mm.sav} layout)?}

\item{max.plausible.age}{warn about siblings whose derived date of birth
implies they would be older than this at the date of interview. Such
rows mean the reported years-since-death and age at death are
jointly inconsistent. Note this is \emph{not} a check on whether a sibling
died before the respondent was born, which is perfectly possible}

\item{death.exposure}{whether a sibling who died contributes the month of
death as exposure. \code{"dhs"} (the default) counts it, matching
\code{Chap16_AM/AM_rates.do}; \code{"mics"} stops the month before, matching
the MICS6 tabulation syntax. The two references genuinely disagree
here, so it cannot be settled by getting it "right"}
}
\value{
a prepped sibling dataset, used by the \verb{prep_*_sib_histories()} functions
}
\description{
Shared by \link{prep_dhs_sib_histories},
\link{prep_nrsim_sib_histories} and
\link{prep_mics_sib_histories}.
}
